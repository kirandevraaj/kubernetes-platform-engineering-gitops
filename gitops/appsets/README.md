# ApplicationSets

Reserved for Argo CD ApplicationSet definitions when one generator must cover multiple targets. The local lab uses a single Application today (`../applications/platform-lab-local.yaml`).
