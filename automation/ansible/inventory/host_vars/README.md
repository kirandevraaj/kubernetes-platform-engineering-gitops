# Place optional per-host overrides here, e.g. localhost.yml
# Example (do not commit secrets):
#   ansible_lab_workdir: C:/Users/you/AppData/Local/Temp/ansible-platform-lab
#
# host_vars beat group_vars but lose to play vars and --extra-vars.
